Hello,

Build #<%= @build.position %> of plan <%= @build.name %> failed (status <%= @build.status %>).

See

  http://<%= TinyCI::Config.site_root %><%= plan_build_path(@build.plan, @build) %>

Sincerely,
TinyCI
