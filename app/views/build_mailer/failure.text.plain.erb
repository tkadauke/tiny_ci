Hello,

Build #<%= @build.position %> of plan <%= @build.name %> failed (status <%= @build.status %>).

See

  http://<%= TinyCI::Config.site_root %><%= project_plan_build_path(@build.project, @build.plan, @build) %>

Sincerely,
TinyCI
