<%= juggernaut :channels => ["queue"] %>

<h1><%= t('.builds_of_plan', :plan_link => link_to(@plan.name, project_plan_path(@project, @plan))) %></h1>

<%= auto_update 'queue' %>
<div id="queue">
  <%= render :partial => 'list', :locals => { :builds => @builds } %>
</div>

<p><%= link_to t('.back_to_plan'), project_plan_path(@project, @plan) %></p>
