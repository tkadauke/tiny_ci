<%= tag.div(
      "",
      data: {
        react_page: "EditProjectPage",
        props: {
          project: {
            name: @project.name,
            description: @project.description
          }
        }.to_json
      }
    ) %>
