<h2>Edit <%= @user.login %>'s Profile</h2>

<% form_for @user do |f| %>
  <%= f.error_messages %>

  <p class="form_item">
    <span class="label"><%= f.label :email, 'E-Mail Address' %></span>
    <span class="desc">The email address is used for notifications.</span>
    <%= f.text_field :email %>
  </p>
  
  <%= f.submit 'Update' %>
<% end %>
